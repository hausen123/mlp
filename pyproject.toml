[project]
name = "mlp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "faiss-gpu-cu12",
    "tqdm",
    "numpy",
    "accelerate",
    "safetensors",
]
